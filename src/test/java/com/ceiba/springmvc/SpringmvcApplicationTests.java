package com.ceiba.springmvc;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class SpringmvcApplicationTests {

	@Test
	void contextLoads() {
		/*Time initialTime = new Time();
		Time now = new Time();

		//1er: No me enseña que compone a una hora
		initialTime.setHour(19);
		initialTime.setMinute(0);
		initialTime.setSecond(0);//2do: recien es una hora

		now.setMinute(0);
		now.setSecond(0);

		now.setHour(19);*/

	}


}
